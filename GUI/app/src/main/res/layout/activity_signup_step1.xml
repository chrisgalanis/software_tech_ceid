<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:padding="16dp"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!-- Close button -->
    <ImageButton
        android:id="@+id/btnClose"
        android:layout_width="32dp"
        android:layout_height="32dp"
        android:background="@null"
        android:src="@android:drawable/ic_menu_close_clear_cancel"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"/>

    <!-- Title -->
    <TextView
        android:id="@+id/tvTitle"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="My first name is"
        android:textSize="24sp"
        android:textStyle="bold"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toEndOf="@id/btnClose"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="0.0"/>

    <!-- First name -->
    <EditText
        android:id="@+id/etFirstName"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:hint="First name"
        android:inputType="textPersonName"
        app:layout_constraintTop_toBottomOf="@id/tvTitle"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>

    <!-- Last name -->
    <TextView
        android:id="@+id/tvLastNameLabel"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="My last name is"
        android:textSize="24sp"
        android:textStyle="bold"
        app:layout_constraintTop_toBottomOf="@id/etFirstName"
        app:layout_constraintStart_toStartOf="parent"
        android:layout_marginTop="24dp"/>

    <EditText
        android:id="@+id/etLastName"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:hint="Last name"
        android:inputType="textPersonName"
        app:layout_constraintTop_toBottomOf="@id/tvLastNameLabel"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>

    <!-- Add photos instruction -->
    <TextView
        android:id="@+id/tvAddPhotosLabel"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Add photos (at least 2)"
        android:textSize="20sp"
        android:textStyle="bold"
        app:layout_constraintTop_toBottomOf="@id/etLastName"
        android:layout_marginTop="24dp"
        app:layout_constraintStart_toStartOf="parent"/>

    <!-- Photo slots -->
    <LinearLayout
        android:id="@+id/llPhotos"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:layout_marginTop="8dp"
        app:layout_constraintTop_toBottomOf="@id/tvAddPhotosLabel"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <!-- We’ll generate these ImageViews in code or define three here -->
        <ImageView
            android:id="@+id/imgPhoto1"
            android:layout_width="80dp"
            android:layout_height="80dp"
            android:src="@drawable/ic_add_photo"
            android:scaleType="centerCrop"
            android:padding="8dp"
            android:background="@drawable/photo_placeholder"/>

        <ImageView
            android:id="@+id/imgPhoto2"
            android:layout_width="80dp"
            android:layout_height="80dp"
            android:src="@drawable/ic_add_photo"
            android:scaleType="centerCrop"
            android:padding="8dp"
            android:layout_marginStart="16dp"
            android:background="@drawable/photo_placeholder"/>

        <ImageView
            android:id="@+id/imgPhoto3"
            android:layout_width="80dp"
            android:layout_height="80dp"
            android:src="@drawable/ic_add_photo"
            android:scaleType="centerCrop"
            android:padding="8dp"
            android:layout_marginStart="16dp"
            android:background="@drawable/photo_placeholder"/>
    </LinearLayout>

    <!-- Continue button -->
    <Button
        android:id="@+id/btnContinueStep1"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="CONTINUE 1/2"
        android:enabled="false"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>

</androidx.constraintlayout.widget.ConstraintLayout>
